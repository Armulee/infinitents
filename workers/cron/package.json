{
  "name": "infinitents-cron",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that drives Infinitents' scheduled jobs (queue tick + daily autopilot) via Cron Triggers.",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.8.2",
    "wrangler": "^3.99.0"
  }
}
